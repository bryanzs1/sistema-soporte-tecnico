databases:
  - name: soporte-tecnico-db
    plan: free
    region: oregon
    postgresMajorVersion: "17"
    databaseName: soporte_db
    user: soporte_user

services:
  - type: web
    name: soporte-tecnico
    runtime: python
    plan: free
    pythonVersion: 3.11
    buildCommand: bash build.sh
    preDeployCommand: python migrate_to_production.py
    startCommand: gunicorn -k gthread --threads 8 -w 1 --bind 0.0.0.0:$PORT wsgi:app
    envVars:
      - key: RENDER
        value: 'true'
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: 'false'
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: soporte-tecnico-db
          property: connectionString
      - key: STRIPE_API_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: STRIPE_DEFAULT_PRICE_ID
        sync: false
      - key: REDIS_URL
        sync: false
      - key: DEFAULT_ADMIN_USERNAME
        sync: false
      - key: DEFAULT_ADMIN_PASSWORD
        sync: false
      - key: DEFAULT_ADMIN_EMAIL
        sync: false
